# Add to saas-db/requirements.txt for production observability.
prometheus-client>=0.20
redis>=5.0                       # readiness ping + (future) Redis-backed rate limiter

# Optional — install only if OTEL_ENABLED=true (tracing):
# opentelemetry-sdk
# opentelemetry-exporter-otlp
# opentelemetry-instrumentation-fastapi
# opentelemetry-instrumentation-sqlalchemy
# opentelemetry-instrumentation-httpx
